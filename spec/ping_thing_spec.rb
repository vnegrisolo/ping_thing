require 'spec_helper'

describe PingThing do
  it 'has a version number' do
    expect(PingThing::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
